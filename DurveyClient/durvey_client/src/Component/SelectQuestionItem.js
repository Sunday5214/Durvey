import React from 'react';
import styled from 'styled-components';
import { QuestionItemLayout, StyledQuestionContent } from './QuestionItemLayout';

const OptionItem = ({optionContent}) => {
    return (
        
    )
}